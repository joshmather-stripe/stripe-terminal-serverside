<div class="font-semibold text-xl mb-4" style="">{{test}}</div>
<div class="grid grid-cols-2 gap-4">
   Terminal Info:
   ID: {{id}}
   Location: {{location}}
   Serial Number: {{serial_number}}
   <form class="w-full max-w-sm" action="/simulate-payment" method="POST">
      <button class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded" type="submit">
      Simulate Payment 
      </button>
   </form>
</div>
